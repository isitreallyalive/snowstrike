{
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug Snowstrike",
            "cargo": {
                "args": [
                    "build"
                ]
            },
            "args": [],
            "cwd": "${workspaceFolder}",
            "env": {
                "CARGO_MANIFEST_DIR": "${workspaceFolder}"
            }
        }
    ]
}
